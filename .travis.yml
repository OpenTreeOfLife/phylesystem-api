language: python
python:
    - "2.7"
    - "3.3"
    - "pypy"

install:
   - pip install -r requirements.txt --use-mirrors

script: nosetests tests/

notifications:
  irc:
    channels: "chat.freenode.net#otol"
  recipients:
    - duke@leto.net
  email:
    on_success: change
    on_failure: always
#env:
